extends controller
block bottomrow
  div.row.subbutton.bg-next
    each val in [1,2,3,4]
      div.col-md-3.col-sm-3.col-xs-3.text-center.subbutton
        div.tbcell(
          onclick = 'setstate({top:'+(now - 1)+',verb:'+states.STOPPED+'})'
          ng-click="clicked=true")
          h1= val
